#オートリンク仕様（フューチャーフォン）

* ページ内のリンクをモバイルプレフィックス付きのURLに自動的に書き換える仕組。
* リンクのURLパラメーターに mobile=off を付ける事で対象リンクのみ書き換えを回避する事ができる
* 全体的に書き換えない場合は、設定ファイルで設定を上書きする

```
$config = array(
	'BcAgent' => array(
		'mobile' => array(
			'autoLink' => false
		)
	)
);
```